/*V1
function sum(x,y){
    return x+y;
}
*/

/*V2
const sum = function(x,y){
    return x+y;
}
*/

//V3
export const sumNum = (x, y) => x + y;
export const divNum = (x, y) => x / y;

//export default (x,y)=>x*y;
const carp = (x,y)=>x*y;
export default carp;

